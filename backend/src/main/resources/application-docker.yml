spring:
  datasource:
    url: jdbc:h2:file:/app/data/votingdb;AUTO_SERVER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  h2:
    console:
      enabled: false
  jpa:
    hibernate:
      ddl-auto: update

logging:
  level:
    com.voting: INFO
